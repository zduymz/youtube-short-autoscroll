{
  "name": "youtube-shorts-autoscroll",
  "version": "1.0.0",
  "description": "Browser extension that automatically scrolls to the next video when a YouTube Short ends",
  "scripts": {
    "generate-icons": "node scripts/generate-icons.js",
    "build:chrome": "node scripts/build.js chrome",
    "build:firefox": "node scripts/build.js firefox",
    "build:all": "npm run build:chrome && npm run build:firefox",
    "package:chrome": "cd dist/chrome && zip -r ../youtube-shorts-autoscroll-chrome.zip .",
    "package:firefox": "cd dist/firefox && zip -r ../youtube-shorts-autoscroll-firefox.zip .",
    "package:all": "npm run build:all && npm run package:chrome && npm run package:firefox"
  },
  "keywords": [
    "youtube",
    "shorts",
    "browser-extension",
    "chrome",
    "firefox"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "fs-extra": "^11.1.1",
    "sharp": "^0.32.6"
  }
}

